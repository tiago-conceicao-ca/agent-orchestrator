{
  "name": "@contaazul/cahi-sdlc",
  "version": "0.0.0",
  "description": "SDLC orchestrator — multi-phase workflow engine over agent-orchestrator",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && node --input-type=commonjs -e \"require('fs').cpSync('src/gates/prompts','dist/gates/prompts',{recursive:true})\"",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@contaazul/cahi-core": "workspace:*",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^25.2.3",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
